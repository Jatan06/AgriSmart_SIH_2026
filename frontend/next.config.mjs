/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: {
    appIsrStatus: false,
    buildActivity: false,
  },
  output: 'export',
  trailingSlash: true,
  basePath: '/AgriSmart_SIH_2026',
};

export default nextConfig;
